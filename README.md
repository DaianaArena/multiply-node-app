# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Toma la base para armar la tabla           [número] [requerido]
  -l, --listar   Lista la tabla pedida en consola                     [booleano]
  -h, --hasta    Hasta que número multiplicar por la base [número] [defecto: 10]
```
